name: Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-linux:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          cache: ${{ !env.ACT }}

      - name: Build for Linux
        run: cargo build --release --target x86_64-unknown-linux-gnu --verbose

      - name: Upload Linux binary
        if: ${{ !env.ACT }}
        uses: actions/upload-artifact@v4
        with:
          name: pathfinder-linux-x64
          path: target/x86_64-unknown-linux-gnu/release/pathfinder

  # build-macos:
  #   runs-on: macos-latest
  #   steps:
  #     - uses: actions/checkout@v4

  #     - name: Install Rust
  #       uses: actions-rust-lang/setup-rust-toolchain@v1

  #     - name: Build for macOS
  #       run: cargo build --release --verbose

  #     - name: Upload macOS binary
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: path-finder-macos-arm64
  #         path: target/release/path-finder
